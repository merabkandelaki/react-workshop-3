const home = "/";
const universitiesCountry = "/universities/:country";
const universityName = "/university/:name";
const customError = "/custom-error"

const routes = {
  home,
  universitiesCountry,
  universityName,
  customError,
};

export default routes;
